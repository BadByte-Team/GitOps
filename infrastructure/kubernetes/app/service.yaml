apiVersion: v1
kind: Service
metadata:
  name: curso-gitops-svc
  namespace: curso-gitops
  labels:
    app: curso-gitops
spec:
  type: LoadBalancer
  selector:
    app: curso-gitops
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
